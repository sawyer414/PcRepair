# Build Photos

Drop your build photos in this folder, then update the `src` attributes in `builds.html`.

For example:
- `build-1.jpg` → used in the first build card
- `build-2.jpg` → used in the second build card
- etc.

Transfer photos from your phone (AirDrop, USB cable, Google Photos download, etc.) and save them here.
